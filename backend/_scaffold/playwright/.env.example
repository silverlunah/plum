BASE_URL=https://www.saucedemo.com/v1/
# IS_HEADLESS only affects runs you start yourself. Set it false to watch the
# browser while debugging. Server and node runs always force headless (no display).
IS_HEADLESS=false
